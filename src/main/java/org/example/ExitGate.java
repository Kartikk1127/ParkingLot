package org.example;

public class ExitGate {
    private Ticket ticket;
    //cost computation
    private CostComputation costComputation; //is it two or four wheeler
    //also needs a parking spot manager so that it could be used to free up the spot
}
